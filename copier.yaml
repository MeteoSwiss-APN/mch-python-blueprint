_subdirectory: "tmpl"
_templates_suffix: ""

full_name: "Monty Python"
email: "{{ full_name.lower().replace(' ', '.') }}@meteoswiss.ch"
github_username: "{{full_name.lower().replace(' ', '_') }}"
project_name: "Flying Circus"
project_slug: "{{ project_name.lower().replace(\"'\", '').replace(' ', '_').replace('-', '_') }}"
project_short_description: "{{ full_name }}'s {{ project_name }}"
project_github: "MeteoSwiss-APN/{{ project_slug }}"
version: "0.1.0"
python_version: "3.10"

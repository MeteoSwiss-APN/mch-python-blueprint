_subdirectory: "tmpl"
_templates_suffix: ".j2"

full_name:
  default: "Monty Python"
email:
  default: "{{ full_name.lower().replace(' ', '.') }}@meteoswiss.ch"
github_username:
  default: "{{ full_name.lower().replace(' ', '_') }}"
project_name:
  default: "Flying Circus"
project_slug:
  default: "{{ project_name.lower().replace(\"'\", '').replace(' ', '_').replace('-', '_') }}"
project_short_description:
  default: "{{ full_name }}'s {{ project_name }}"
project_github:
  default: "MeteoSwiss-APN/{{ project_slug }}"
version:
  default: "0.1.0"
python_version:
  default: "3.10"
year:
  default: "2022"
